packageMetadata:
  - repository: qlik-trial/tala-agent
    type: docker
    name: tala-agent
    apiComplianceConfig:
      specPaths:
        - {spec_path} ## TODO: Replace {spec_path} with actual path
    depConfig:
      pythonDepSpecs:
        - requirements.txt
  - repository: qlik-trial/tala-agent
    type: helm
    name: helm/tala-agent
    depConfig:
      helmDepSpecs:
        - manifests/chart/tala-agent
